<% provide(:title, 'All users') %>
<h1>All users</h1>
<div class="col col-md-6 offset-md-3">
  <ul class="users">
    <%= paginate @users %>
    <%= render @users %>
    <%= paginate @users %>
  </ul>
</div>
